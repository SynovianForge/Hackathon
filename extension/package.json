{
  "name": "gatekeeper",
  "displayName": "GateKeeper",
  "description": "Pop quiz gatekeeper — prove you understand your code before every commit.",
  "version": "0.0.1",
  "publisher": "synovianforge",
  "repository": "https://github.com/SynovianForge/Hackathon",
  "engines": { "vscode": "^1.80.0" },
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {},
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.80.0",
    "typescript": "^5.0.0"
  }
}
